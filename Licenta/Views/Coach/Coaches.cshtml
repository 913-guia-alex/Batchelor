@model List<Licenta.Models.Coach>

@{
    ViewBag.Title = "Coaches";
}

<!-- Add these scripts before the closing </body> tag -->
<script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" />


<style>
    /* Add this style block in the head section of your HTML */
    .btn-transition {
        transition: background-color 0.3s ease;
    }

        .btn-transition:hover {
            background-color: forestgreen;
            color: white;
        }

    /* Adjusted style for search bar alignment */
    .search-container {
        margin-bottom: 10px;
        display: flex;
        justify-content: flex-end; /* Align to the right */
    }

        .search-container input {
            width: 200px; /* Adjust as needed */
            margin-left: 10px;
        }








    .dropdown {
        height: fit-content;
        width: 100px;
        position: absolute;
        display: flex;
        top: 115px; /* Adjusted top position */
        left: 215px; /* Adjusted left position */
        z-index: 1000; /* Ensure the dropdown is above other elements */
    }

    .dropdown-toggle {
        color: #333;
        padding: .75rem 2.5rem;
        border: none;
        border-radius: .5rem;
        cursor: pointer;
        width: 150px;
        display: flex;
        align-items: center; /* Center items vertically */
        justify-content: space-between; /* Add space between text and icon */
    }

        .dropdown-toggle span {
            display: flex;
            align-items: center; /* Center items vertically */
        }

        .dropdown-toggle i {
            margin: 0.3rem; /* Adjust spacing between text and icon */
        }

    .dropdown-menu {
        position: absolute;
        top: 100%; /* Adjusted position to be under the dropdown button */
        left: 0; /* Adjusted position to be aligned with the left side */
        border: 1px solid #ccc;
        border-radius: 1rem;
        padding: 0;
        list-style: none;
        display: none;
    }

        .dropdown-menu .menu-item:first-child {
            border-radius: 1rem 1rem 0 0;
        }

        .dropdown-menu .menu-item:last-child {
            border-radius: 0 0 1rem 1rem;
        }

    .menu-items {
        width: 170px;
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 1rem 1.5rem; /* Adjusted padding */
        border: none;
        font-size: 1.1rem;
        cursor: pointer;
        border-radius: 13px !important; /* Remove button border-radius for a rectangular shape */
    }








    /* Modal styles */
    .modal-container {
        display: none;
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.5);
        align-items: center;
        justify-content: center;
        z-index: 1;
    }

    .modal-content {
        background-color: #fff;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        max-width: 400px;
        width: 100%;
        padding: 20px;
        text-align: center;
    }

    .button-container {
        display: flex;
        justify-content: space-around;
    }

    .button {
        padding: 10px 20px;
        font-size: 16px;
        text-align: center;
        text-decoration: none;
        border-radius: 5px;
        cursor: pointer;
    }
        /* Add hover animations to the buttons */
        .button:hover {
            transform: scale(1.05); /* Scale the button slightly on hover */
            transition: transform 0.3s ease;
        }


    .cancel-button {
        background-color: #ccc;
        color: #535353;
    }

        .cancel-button:hover {
            background-color: #bbb; /* Darken the background color on hover */
        }

    .delete-button {
        background-color: #e74c3c;
        color: #fff;
    }

        .delete-button:hover {
            background-color: #c0392b; /* Darken the background color on hover */
        }
</style>

<h2 style="text-align: center;">Admin Panel - Coaches</h2>

<div class="container">
    <!-- Filter Dropdown -->
    <div class="dropdown">
        <div class="dropdown-toggle" onclick="toggleDropdown()">
            <span>
                <i class="fas fa-sort"></i> <!-- Sort icon -->
                Sort
            </span>
        </div>
        <ul class="dropdown-menu" id="dropdownMenu">
            <li class="menu-items" onclick="handleButtonClick('sortByName')">
                <i class="fas fa-sort-alpha-down"></i>Name
            </li>
            <li class="menu-items" onclick="handleButtonClick('sortByAge')">
                <i class="fas fa-sort-alpha-down"></i>Age
            </li>
            <li class="menu-items" onclick="handleButtonClick('sortByTrainerType')">
                <i class="fas fa-sort-alpha-down"></i>Trainer Type
            </li>
        </ul>
    </div>

    <!-- Search Bar -->
    <div class="search-container">
        <input type="text" id="searchInput" placeholder="Search..." class="form-control">
    </div>

    <!-- Coach List Table -->
    <table class="table">
        <tr>
            <th>#</th>
            <th>Name</th>
            <th>Age</th>
            <th>Gender</th>
            <th>Trainer Type</th>
            <th>Phone Number</th>
            <th>Email</th>
            <th>Photo</th>
            <th>Action</th>
        </tr>
        @foreach (var coach in Model)
        {

            <tr>
                <td>@coach.idco</td>
                <td>@coach.name</td>
                <td>@coach.age</td>
                <td>@coach.gender</td>
                <td>@coach.trainerType</td>
                <td>@coach.phoneNumber</td>
                <td>@coach.email</td>
                <td>
                    <!-- Assuming the photo is stored as base64 string -->
                    <img src="data:image;base64,@Convert.ToBase64String(coach.photo)" alt="Coach Photo" width="50" height="50" />
                </td>
                <td>
                    <button class="btn btn-danger" onclick="showDeleteConfirmation(@coach.idco)">Delete</button>
                    <a href="/Coach/UpdateCoachView?id=@coach.idco" class="btn btn-primary">Update</a>
                </td>
            </tr>
        }
    </table>

    <div class="row">
        <div class="col-md-6">
            <a href="/Coach/AddCoach" class="btn btn-secondary btn-transition">Add a new coach</a>
        </div>
        <div class="col-md-6 text-md-right">
            <a href="/Admin/Admins" class="btn btn-secondary btn-transition">Go Back</a>
        </div>
    </div>
</div>


<!-- Delete Confirmation Modal -->
<div id="deleteModal" class="modal-container">
    <div class="modal-content">
        <h2>Delete Confirmation</h2>
        <p class="confirmation-message">
            Are you sure you want to delete this item?
        </p>
        <div class="button-container">
            <button id="cancelDeleteBtn" class="button cancel-button">
                Cancel
            </button>
            <button id="confirmDeleteBtn" class="button delete-button">
                Delete
            </button>
        </div>
    </div>
</div>

<script>
    function toggleDropdown() {
        var dropdownMenu = document.getElementById("dropdownMenu");
        dropdownMenu.style.display = (dropdownMenu.style.display === "none" || dropdownMenu.style.display === "") ? "block" : "none";
    }

    function handleButtonClick(action) {
        toggleDropdown();
        if (action === "sortByName") {
            sortByName();
        } else if (action === "sortByAge") {
            sortByAge();
        } else if (action === "sortByTrainerType") {
            sortByTrainerType();
        }
    }

    function sortByAge() {
        var rows = $("table tbody tr").get();

        rows.sort(function (rowA, rowB) {
            var ageA = parseInt($(rowA).find("td:eq(2)").text());
            var ageB = parseInt($(rowB).find("td:eq(2)").text());

            return ageA - ageB;
        });

        $.each(rows, function (index, row) {
            $("table tbody").append(row);
        });
    }

    function sortByTrainerType() {
        var rows = $("table tbody tr").get();

        rows.sort(function (rowA, rowB) {
            var trainerTypeA = $(rowA).find("td:eq(4)").text();
            var trainerTypeB = $(rowB).find("td:eq(4)").text();

            return trainerTypeA.localeCompare(trainerTypeB);
        });

        $.each(rows, function (index, row) {
            $("table tbody").append(row);
        });
    }

    // Function to sort gyms by name (alphabetically)
    function sortByName() {
        var rows = $("table tbody tr").get();

        rows.sort(function (rowA, rowB) {
            var nameA = $(rowA).find("td:eq(1)").text().toUpperCase(); // Extract name value from cell and convert to uppercase for case-insensitive comparison
            var nameB = $(rowB).find("td:eq(1)").text().toUpperCase(); // Extract name value from cell and convert to uppercase for case-insensitive comparison

            return nameA.localeCompare(nameB); // Sort alphabetically
        });

        $.each(rows, function (index, row) {
            $("table tbody").append(row);
        });
    }

    // Function to filter customers based on search input
    $(document).ready(function () {
        $("#searchInput").on("keyup", function () {
            var value = $(this).val().toLowerCase();
            $("table tr").filter(function () {
                $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
            });
        });
    });

    function showDeleteConfirmation(gymId) {
        const modal = document.getElementById('deleteModal');
        modal.style.display = 'flex';
        const confirmDeleteBtn = document.getElementById('confirmDeleteBtn');
        confirmDeleteBtn.onclick = function () {
            deleteGym(gymId);
            hideModal('deleteModal');
        }
        const cancelDeleteBtn = document.getElementById('cancelDeleteBtn');
        cancelDeleteBtn.onclick = function () {
            hideModal('deleteModal');
        }
    }

    function hideModal(modalId) {
        const modal = document.getElementById(modalId);
        modal.style.display = 'none';
    }

    function deleteCoach(coachId) {
        $.ajax({
            type: "POST",
            url: "/Coach/DeleteCoach",
            data: { id: coachId },
            success: function () {
                // Refresh the coach list (replace with your actual refresh logic)
                location.reload();
            }
        });
    }


    $(document).on("click", function (event) {
        if (!$(event.target).closest(".dropdown").length && $("#dropdownMenu").css("display") === "block") {
            toggleDropdown(); // Retract dropdown menu if it's shown
        }
    });


</script>
